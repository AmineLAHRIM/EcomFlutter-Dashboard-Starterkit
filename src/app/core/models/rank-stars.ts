export class RankStars {
    fraction: number;
    numbers = [];
    leftedNumbers = [];


    constructor(fraction: number, numbers: any[], leftedNumbers: any[]) {
        this.fraction = fraction;
        this.numbers = numbers;
        this.leftedNumbers = leftedNumbers;
    }

}